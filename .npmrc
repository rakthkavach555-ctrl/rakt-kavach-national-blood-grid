# Cleaned .npmrc — removed Replit-specific settings.
# Replit puts project-specific npm config here; to keep the repo clean we removed Replit-specific flags.
# If you need npm config for CI or local development, add it in your CI environment or a local-only .npmrc (do NOT commit credentials).
